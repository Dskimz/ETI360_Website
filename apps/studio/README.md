# ETI360 Sanity Studio

## Setup

1. Copy `apps/studio/.env.local.example` → `apps/studio/.env.local`
2. Fill in:
   - `SANITY_STUDIO_PROJECT_ID`
   - `SANITY_STUDIO_DATASET`

## Run

From repo root:

- `npm run dev:studio`

